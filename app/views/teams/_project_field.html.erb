<% @team.projects.each do |f| %>
  <tr>
    <td><%= f.name %></td>
    <td><%= f.abb %></td>
    <td><%= f.users.count %></td>
    <td><%= link_to "", edit_team_path(@team), class: "glyphicon glyphicon-edit" %>
    </td>
  </tr>
<% end %>
