<h1>All user in My company </h1> 
<%= will_paginate %>
<%= link_to "Add", new_admin_user_path, class: "glyphicon glyphicon-plus" %>
<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
        <th>Birthday</th>
        <th>Setting</th>
      </tr>
    </thead>
    <tbody>
      <%= render @users %>
    </tbody>
  </table>
</div>
<%= will_paginate %>

<%= link_to "Back", root_path, class: "glyphicon glyphicon-step-backward" %>

